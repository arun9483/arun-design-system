---
'@arun-dev/ui': patch
---

Require `@arun-dev/headless` >= 3.0.0. The previous `>=1.0.0` range let a consumer satisfy the peer
with a headless version that predates the render engine `@arun-dev/ui` 3.x is built on.
