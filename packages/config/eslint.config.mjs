// Internal shim — the canonical lint policy lives in @arun-dev/eslint-config
// (packages/eslint-config), published to npm for every arun-dev frontend repo.
export { baseConfig, reactConfig, ignores } from '@arun-dev/eslint-config';
export { default } from '@arun-dev/eslint-config';
